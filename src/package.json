// CHANGE: Added proper package configuration with all dependencies
// REASON: Original package.json was missing required dependencies
// ORIGINAL: (line ~1) Missing dependencies and scripts

{
  "name": "deepseek-api-client",
  "version": "1.0.0",
  "description": "Production-ready DeepSeek API client with error handling and rate limiting",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "eslint": "^8.50.0",
    "eslint-config-standard": "^17.1.0"
  },
  "keywords": [
    "deepseek",
    "api",
    "ai",
    "chat",
    "client"
  ],
  "license": "MIT",
  "engines": {
    "node": ">=14.0.0"
  }
}
```

